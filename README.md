Vehicle Classifier

A Convolutional Neural Network (CNN) based vehicle classifier that can identify Bus, Car, Truck, and Motorcycle from images.
The project includes:

Training a CNN from scratch

Data preprocessing & augmentation

Real-time image prediction via FastAPI

GPU support if available



features

Custom CNN model trained on your vehicle dataset

Data augmentation: rotation, horizontal flip, resize

Train/Test split automation

GPU acceleration if available

FastAPI endpoint for image predictions
